{
  "name": "xnxx-api-jsk",
  "version": "1.0.0",
  "scripts": {
   "build": "esbuild index.js --bundle --outfile=dist/_worker.js --format=esm"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
